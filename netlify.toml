[build]
  command = "npm run build"
  publish = "dist"
  functions = "netlify/functions"

# Specific redirects MUST come before the catch-all



[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

